#!/bin/sh

D=`realpath $0`
EVA_DIR=`dirname ${D}`/..
EVA_DIR=`realpath ${EVA_DIR}`

PYTHON=${EVA_DIR}/python3/bin/python3

ARGS=$*
[ ! "$ARGS" ] && [ "`basename $0`" = "eva-shell" ] && ARGS=-I

${PYTHON} ${EVA_DIR}/bin/eva-shell.py ${ARGS}
