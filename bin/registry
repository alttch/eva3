#!/usr/bin/env bash

D=`realpath $0`
EVA_DIR=`dirname ${D}`/..

REGISTRY_INI=${EVA_DIR}/etc/eva_registry

[ -f ${REGISTRY_INI} ] && source ${REGISTRY_INI}

[ -z ${SOCKET} ] && SOCKET=$EVA_DIR/var/registry.sock

$EVA_DIR/yedb/yedb-cli -C $SOCKET $@
