# build virtualenv with system pip3 (should be installed)
#USE_SYSTEM_PIP=1
# use system packages if available
#SYSTEM_SITE_PACKAGES=1
# skip packages, space separated (enable system site packages to let venv use
# preinstalled)
#SKIP="pandas"
