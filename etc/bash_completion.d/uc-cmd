_uc-cmd_server() {
  local cur prev words cword
  _init_completion || return
  if [[ $cword -eq 2 ]]; then
    COMPREPLY=( $( compgen -W 'reload restart start status stop' -- "$cur" ))
  fi
}

_uc-cmd_driver() {
  local cur prev words cword
  _init_completion || return
  if [[ $cword -eq 2 ]]; then
    COMPREPLY=( $( compgen -W 'assign get list load set unassign unload' -- "$cur" ))
  fi
}

_uc-cmd_lpi() {
  local cur prev words cword
  _init_completion || return
  if [[ $cword -eq 2 ]]; then
    COMPREPLY=( $( compgen -W 'modhelp modinfo mods' -- "$cur" ))
  fi
}

_uc-cmd_phi() {
  local cur prev words cword
  _init_completion || return
  if [[ $cword -eq 2 ]]; then
    COMPREPLY=( $( compgen -W 'download exec get list load modhelp modinfo mods set test unlink unload' -- "$cur" ))
  fi
}

_uc-cmd_owfs() {
  local cur prev words cword
  _init_completion || return
  if [[ $cword -eq 2 ]]; then
    COMPREPLY=( $( compgen -W 'create destroy list scan test' -- "$cur" ))
  fi
}

_uc-cmd_modbus() {
  local cur prev words cword
  _init_completion || return
  if [[ $cword -eq 2 ]]; then
    COMPREPLY=( $( compgen -W 'create destroy list test' -- "$cur" ))
  fi
}

_uc-cmd_device() {
  local cur prev words cword
  _init_completion || return
  if [[ $cword -eq 2 ]]; then
    COMPREPLY=( $( compgen -W 'deploy templates undeploy update' -- "$cur" ))
  fi
}

_uc-cmd_clone() {
  local cur prev words cword
  _init_completion || return
  if [[ $cword -eq 2 ]]; then
    COMPREPLY=( $( compgen -W 'group item' -- "$cur" ))
  fi
}

_uc-cmd_config() {
  local cur prev words cword
  _init_completion || return
  if [[ $cword -eq 2 ]]; then
    COMPREPLY=( $( compgen -W 'get props save set' -- "$cur" ))
  fi
}

_uc-cmd_edit() {
  local cur prev words cword
  _init_completion || return
  if [[ $cword -eq 2 ]]; then
    COMPREPLY=( $( compgen -W 'action template update' -- "$cur" ))
  fi
}

_uc-cmd_action() {
  local cur prev words cword
  _init_completion || return
  if [[ $cword -eq 2 ]]; then
    COMPREPLY=( $( compgen -W 'clear disable enable exec kill result terminate toggle' -- "$cur" ))
  fi
}

_uc-cmd_user() {
  local cur prev words cword
  _init_completion || return
  if [[ $cword -eq 2 ]]; then
    COMPREPLY=( $( compgen -W 'create destroy key list password' -- "$cur" ))
  fi
}

_uc-cmd_key() {
  local cur prev words cword
  _init_completion || return
  if [[ $cword -eq 2 ]]; then
    COMPREPLY=( $( compgen -W 'create destroy list props regenerate set' -- "$cur" ))
  fi
}

_uc-cmd_file() {
  local cur prev words cword
  _init_completion || return
  if [[ $cword -eq 2 ]]; then
    COMPREPLY=( $( compgen -W 'create get mod unlink upload' -- "$cur" ))
  fi
}

_uc-cmd_cvar() {
  local cur prev words cword
  _init_completion || return
  if [[ $cword -eq 2 ]]; then
    COMPREPLY=( $( compgen -W 'all delete get set' -- "$cur" ))
  fi
}

_uc-cmd_log() {
  local cur prev words cword
  _init_completion || return
  if [[ $cword -eq 2 ]]; then
    COMPREPLY=( $( compgen -W 'critical debug error get info rotate warning' -- "$cur" ))
  fi
}

_uc-cmd() {
  local cur prev words cword
  _init_completion || return
  if [[ $cword -eq 1 ]]; then
    COMPREPLY=( $( compgen -W 'action clone cmd config create cvar debug destroy device driver dump edit file history key list lock log lpi modbus owfs phi save server state test unlock update user' -- "$cur" ))
  else
    case "${words[1]}" in
      action|clone|config|cvar|device|driver|edit|file|key|log|lpi|modbus|owfs|phi|server|user)
        _uc-cmd_${words[1]}
        ;;
    esac
  fi
}

complete -F _uc-cmd uc-cmd